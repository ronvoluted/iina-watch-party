{
  "name": "@iina-watch-party/worker",
  "private": true,
  "type": "module",
  "version": "0.0.0",
  "scripts": {
    "build": "wrangler deploy --dry-run --outdir dist",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run"
  },
  "dependencies": {
    "@iina-watch-party/shared": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.8.0",
    "@cloudflare/workers-types": "^4.20250312.0",
    "vitest": "^3.0.0",
    "wrangler": "^4.0.0"
  }
}
